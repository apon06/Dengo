name: dengugo
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0" # sdk update 

dependencies:
  flutter:
    sdk: flutter

  # cupertino_icons: ^1.0.2
  # get: ^4.3.4
  # youtube_player_flutter: ^8.0.0
  flutter_lints: ^4.0.0
  cupertino_icons: ^1.0.8
  get: ^4.6.6
  youtube_player_flutter: ^9.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
